const initialState = {
    lyrics: '',
    isLoading: false
}

export const lyricsReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

